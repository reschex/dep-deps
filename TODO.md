- use git to determine which files and possibly methods have been touched the most over a pre-determined/configurable time frame, say 3 or 6 month and then give those file an additional weighting in the risk score.

- there must be a neat way of visualising the dependencies
- test against python code 
- test against Java code
- publish as actual extension if internal users find it useful?